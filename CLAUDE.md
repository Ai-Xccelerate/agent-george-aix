# Agent George — Project Context for Claude Code

All project context lives in **`AGENTS.md`**. Read that file end-to-end on a fresh context — it covers stack, layout, locked-in decisions, gotchas, and memory status. Then start with `docs/BACKLOG.md` for "what's next?".

@AGENTS.md
